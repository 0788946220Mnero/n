# ملخّص الإصلاحات — الباك اند (ديار الأنباط)

## 🔴 أخطاء حقيقية كانت تُعطّل الخدمة

### 1) تسجيل دخول الزبائن (OTP) كان معطّلاً بالكامل
في `controllers/phoneAuthController.js` كان الكود يستدعي `isValidPhone` و `sendLog`
وهما **غير معرَّفين إطلاقاً** في الملف. النتيجة: كل طلب على
`/api/auth/send-otp` و `/api/auth/verify-otp` يرمي `ReferenceError`
ويعيد **500** للزبون. أي أن الدخول برقم الهاتف لم يكن يعمل أصلاً.

تم تعريف الاثنين:
- `isValidPhone` يتحقق من الرقم بعد التطبيع `+9627XXXXXXXX`
- `sendLog` سجلّ في الذاكرة لتحديد المعدّل، مع تنظيف دوري كي لا ينمو

كما صُحّحت رسالة المهلة (كانت تقول 10 دقائق والنافذة الفعلية 30 ثانية).

### 2) سقوط الخادم عند أي خطأ داخل دالة async
22 دالة في المتحكّمات كانت `async` بلا `try/catch` وبلا غلاف.
في Express 4 لا يلتقط Express رفض الـ Promise، فيتحوّل إلى
`unhandledRejection` — ومع Node 18+ **يسقط الخادم كاملاً** (502 على Railway).
يكفي أن يفتح أحدهم `/api/products/abc` بمعرّف غير صالح ليتوقف الموقع.

أُضيف `utils/asyncHandler.js` ولُفّت به كل المتحكّمات ووسيط `protectCustomer`.
تم التحقق عملياً: الطلب صار يعيد `400 المعرّف المُرسَل غير صالح` والخادم يبقى يعمل.

---

## 🟠 إعداد CORS والنطاق الجديد

كان الإعداد القديم ينتهي بـ `return callback(null, true)` أي **يسمح لأي نطاق**،
فقائمة `CLIENT_URL` كانت بلا أثر (ومع `credentials: true` هذا خطر أمني).

الآن:
- قائمة ثابتة تضم `diyaralanbat.com` و `www.diyaralanbat.com` و
  `diyaralanbat.netlify.app` و `diyaradmin.netlify.app` — تعمل حتى لو نسيت المتغيّر
- أي نطاق إضافي في `CLIENT_URL` يُضاف فوقها
- أي نطاق فرعي على netlify.app و localhost للتطوير
- ترويسة `x-print-token` مسموحة صراحةً (لبرنامج الطباعة)
- `maxAge: 86400` لتخزين نتيجة preflight — استجابة أسرع للموقع
- النطاق غير المسموح يُرفض بـ 403 ورسالة عربية واضحة في السجلات

**helmet**: كان الإعداد الافتراضي يضع `Cross-Origin-Resource-Policy: same-origin`
و CSP — وكلاهما بلا معنى في خادم API وقد يعيق الموارد عبر النطاقات.
صار: CSP معطّل، و CORP = `cross-origin`.

**`/api/health`** صار يُرجع النطاق الطالب وقائمة النطاقات المسموحة —
افتحه من `diyaralanbat.com` لتتأكد فوراً أن الربط سليم.

---

## 🟡 تحسينات أخرى

- **معالج الأخطاء**: رسائل عربية دقيقة لـ CastError و ValidationError
  و duplicate key (11000) وأخطاء JWT، ولا يكشف تفاصيل داخلية في الإنتاج
- **حظر الأرقام**: كان يفحص الرقم الخام فقط، فيفلت الحظر إن كتب الزبون الرقم بفراغ
  أو شرطة. صار يفحص الخام والمطبَّع معاً
- **`sharp` أُزيلت** من package.json — لم تعد مستخدمة بعد الانتقال إلى Cloudinary،
  وهي أثقل حزمة في البناء (تسريع ملموس لنشر Railway)
- **express** مثبّتة على `^4.21.2` — الترقية إلى 5 تكسر
  `app.options('*')` و `express-mongo-sanitize`
- **`engines: node >=18 <23`** حتى لا يختار Railway إصداراً يكسر الحزم
- إزالة استيراد `printAuth` غير المستخدم، وحدّ 10mb لـ urlencoded
- `process.on('unhandledRejection' / 'uncaughtException')` لتسجيل أي خطأ طارئ
- ملف `.env.example` بكل المتغيّرات المطلوبة
- `module.exports = app` في server.js لتسهيل الاختبار لاحقاً

---

## ✅ التحقق

- ESLint: **صفر أخطاء** (كان 4)
- `node --check` على كل الملفات: سليمة
- إقلاع فعلي للخادم واختبار CORS من 8 حالات: النطاقات المسموحة 200،
  النطاق الغريب 403، preflight 204
